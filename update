#!/bin/bash

# gits pull origin master
# gem update && gem cleanup
# sudo npm update -g

for dir in `cat .gitslave | awk {'print $2'} | tr -d '"'`
do
  cd $dir
  echo "Performing update in directory $dir"
  [[ -e .giternal.yml ]] && giternal update
  [[ -e update ]] && ./update
  cd -
done